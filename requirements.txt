# ServiceNow → Jira Integration — Python Dependencies
# Install: pip install -r requirements.txt

# Web framework + ASGI server (webhook listener)
fastapi==0.111.0
uvicorn[standard]==0.29.0
gunicorn==22.0.0

# HTTP client (both scripts)
requests==2.31.0

# .env loader (both scripts)
python-dotenv==1.0.1

# Azure Key Vault + SPN auth (both scripts)
azure-identity==1.16.0
azure-keyvault-secrets==4.8.0
